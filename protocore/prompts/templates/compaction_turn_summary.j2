<turn>
{{ turn }}
</turn>

Summarise the above turn in at most {{ max_words }} words (about {{ max_chars }} characters); a longer summary is cut off and discarded, so stay well inside it. Preserve tool names, key user intent, file paths touched, and the exact identifiers the next step will need (paths, ids, ports, URLs, numbers, error codes) verbatim; a long tool result is not copied — say how many records it held and keep the ones that matter; never round, guess or substitute a plausible value, and say so if a result was missing. The absence of a tool result or confirmation in the turn means the outcome is UNKNOWN, not that it did not happen or that it succeeded: state unknowns as unknown. Output STRICT JSON with exactly one key, {"summary": "..."}, and nothing else.